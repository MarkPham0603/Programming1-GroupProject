public class Admin extends User{

    public Admin(String username, String password, String fullname, String email, String address, int phoneNumber) {
        super(username, password, fullname, email, address, phoneNumber);
    }
}
